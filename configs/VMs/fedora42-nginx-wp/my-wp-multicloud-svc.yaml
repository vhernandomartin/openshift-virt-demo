apiVersion: v1
kind: Service
metadata:
  name: my-wp-multicloud
  namespace: my-wordpress
spec:
  selector:
    expose: fedora42-nginx-wp
  type: LoadBalancer
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080