apiVersion: v1
kind: Service
metadata:
  name: my-wp-mysql
  namespace: my-wordpress
  labels:
    expose: fedora42-mysql-wp
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    expose: fedora42-mysql-wp